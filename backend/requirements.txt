# GlassBox backend. Pin in hour 1, freeze before anyone builds on top.
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9

# Model + interpretability (GPU)
torch>=2.4
transformers>=4.44
sae-lens>=6.0            # decoderesearch/SAELens — loads Gemma Scope
scikit-learn>=1.5        # calibrated logistic-regression probe + baseline
datasets>=3.0            # MedMCQA / PubMedQA (offline validation)

# Sponsors (CPU-side, downstream of the hook — never import torch here)
sentry-sdk>=2.0
arize-phoenix>=5.0
openinference-instrumentation>=0.1
anthropic>=0.39
httpx>=0.27              # Neuronpedia label fetch (one-time/offline)
